// Generated by CoffeeScript 1.10.0

/* public/js/app.js */
var app;

app = angular.module('scrumtious', []);

app.controller('StickyButtonCtrl', function($scope) {
  $scope.addSticky = function() {
    return alert('Sticky added!');
  };
  return $scope.mouseover = function() {
    return console.log('Moused over');
  };
});

app.controller('BoardCtrl', function($scope) {
  return $scope.columns = ['Todo', 'In Progress', 'Done'];
});

app.directive('sb-draggable', function() {
  return {
    restrict: 'A',
    compile: function(element, attrs) {}
  };
});

app.directive('sb-sticky', function($document) {
  return function(scope, element, attr) {
    var mousemove, mouseup, startX, startY, template, x, y;
    startX = startY = x = y = 0;
    element.on('mousedown', function(event) {
      event.preventDefault();
      startX = event.screenX - x;
      startY = event.screenY - y;
      $document.on('mousemove', mousemove);
      return $document.on('mouseup', mouseup);
    });
    mousemove = function(event) {
      x = event.screenX - startX;
      y = event.screenY - startY;
      return element.css({
        left: x + 'px',
        top: y + 'px'
      });
    };
    mouseup = function(event) {
      $document.off('mousemove', mousemove);
      return $document.off('mouseup', mouseup);
    };
    return template = '<div sb-draggable>\n  <p>Sticky directive<p>\n</div>';
  };
});
