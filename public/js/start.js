// Generated by CoffeeScript 1.10.0

/* public/js/start.js */
var fingerprint, names, ref, tag;

ref = Cookies.get();
for (fingerprint in ref) {
  names = ref[fingerprint];
  names = JSON.parse(names);
  tag = $('<div>', {
    "class": 'chip white'
  }).append($('<a>', {
    href: "/" + names.cleanBoardName + "/" + fingerprint
  }).css({
    color: 'MediumAquaMarine'
  }).text(names.boardName)).append($('<i>', {
    "class": 'material-icons',
    onclick: "Cookies.remove('" + fingerprint + "');"
  }).css({
    color: 'MediumAquaMarine'
  }).text('close'));
  $('#previous_boards').append(tag);
}

$(function() {
  return setTimeout(function() {
    return $('#boardName').focus();
  }, 100);
});
