// Generated by CoffeeScript 1.10.0

/* public/js/board.js */
var button, initialize, stickyManager;

button = stickyManager = null;

initialize = function(stickyData) {
  stickyManager = new StickyManager(stickyData);
  button = $('<a>', {
    "class": 'btn-floating btn-large waves-effect waves-light red'
  }).css({
    position: 'fixed',
    left: '90%',
    top: '90%'
  }).click(function() {
    return stickyManager.addSticky();
  });
  button.append($('<i>', {
    "class": 'material-icons'
  }).text('+'));
  return $(document.body).append(button);
};

$('html').trap([8], function() {});

$('html').focus();
